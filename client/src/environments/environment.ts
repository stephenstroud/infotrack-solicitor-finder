/**
 * API base URL. Points at the .NET Web API's HTTP dev profile (see the API's launchSettings).
 * CORS on the API allows this SPA's origin (http://localhost:4200).
 */
export const environment = {
  apiBaseUrl: 'http://localhost:5211/api',
};
